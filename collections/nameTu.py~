from collections import namedtuple
n,student,summ,temp = int(input()),namedtuple("student",",".join(input().split())),0,0
for _ in range (n):
    summ+=int(student(*input().split()).MARKS)
print(summ/n)
